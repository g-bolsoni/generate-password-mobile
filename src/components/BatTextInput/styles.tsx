import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        width: '100%',
        height: 40,
        backgroundColor: '#4d4d4d',
        padding: 10,
        borderRadius: 12,
        fontSize: 16,
        color: '#fff',
        marginBottom: 10,
        textAlign: 'center',
    }
});